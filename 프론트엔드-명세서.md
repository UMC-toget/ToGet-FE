# 📄 ToGet Frontend 명세서

> 본 문서는 **`main` 브랜치**의 실제 소스코드를 기준으로 작성되었습니다. (GitHub 교차 검증 완료 · 최종 업데이트: 2026.08.13)
> 저장소: [UMC-toget/ToGet-FE](https://github.com/UMC-toget/ToGet-FE) · 기준 브랜치: `main`

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | ToGet (선물 펀딩 서비스) |
| 플랫폼 | 모바일 우선 반응형 웹 (max-width 402px) |
| 프레임워크 | React 19 + TypeScript + Vite |
| 스타일 | Tailwind CSS v4 (`@theme` 디자인 토큰) |
| 라우팅 | react-router-dom v7 |
| 상태 관리 | Context API(인증) + Zustand(클라이언트) + TanStack Query(서버) |
| 데이터 | 모든 도메인 실제 백엔드 API 연동 (Mock 없음) |

---

## 1. 프로젝트 소개

ToGet은 생일·졸업·집들이 등의 선물을 여러 사람이 함께 준비하는 **선물 펀딩 서비스**입니다.
사용자는 받고 싶은 선물을 위시로 모으고, 선물 페이지(내 선물/함께 선물)를 만들어 초대장을 공유하며, 친구들과 함께 금액을 모으거나 선물 후보를 투표로 정하고, 선물이 전달된 뒤에는 후기·감사 인사를 남길 수 있습니다.

**전체 서비스 흐름**: 스플래시 → 홈(비로그인도 열람 가능) → 로그인(카카오/구글, 신규 가입자는 프로필 설정) → 위시 등록/수정 → 마이페이지(내 정보·계좌 관리) → 선물 만들기(내 선물/함께 선물) → 펀딩 상세/참여/후기 · 함께 선물 참여(후보 투표/정산) → 관리자(상품·초대장 테마 관리).

스플래시가 로그인이 아니라 홈으로 이동하는 이유는 구글 OAuth 브랜딩 심사 요건상 비로그인 사용자도 로그인 없이 서비스 내용을 먼저 볼 수 있어야 하기 때문입니다(`SplashPage.tsx` 주석 참고).

인증·프로필·계좌·위시·펀딩(내 선물/함께 선물)·후기·관리자 기능까지 **모든 도메인이 실제 백엔드 API와 연동**되어 있습니다.

---

## 2. 전체 화면 목록

`main` 브랜치 `App.tsx`에 등록된 라우팅 기준 전체 화면입니다 (담당자는 `git log` 커밋 작성자 기준, 최다 커밋자 표기).

| No | 화면명 | Route Path | Page Component | 담당자 | 주요 기능 |
| --- | --- | --- | --- | --- | --- |
| 1 | 스플래시 | `/` | `SplashPage` | 이홍연 | 진입 GIF 노출, 4초 후 홈으로 자동 이동 |
| 2 | 로그인 | `/login` | `LoginPage` | 이홍연 | 카카오/구글 소셜 로그인, 신규 가입자는 프로필 설정으로 분기 |
| 3 | 프로필 설정 | `/signup/profile` | `ProfileSetupPage` | 이홍연 | 최초 가입자 닉네임·프로필 사진 설정, 약관 동의 후 가입 |
| 4 | 홈 | `/home` | `HomePage` | 이홍연 | 배너(선물 페이지 만들기/로그인) + 선물 둘러보기(카테고리/가격 필터) + 내 펀딩 목록 |
| 5 | 개인정보처리방침 | `/privacy-policy` | `PrivacyPolicyPage` | 이홍연 | 개인정보처리방침 정적 텍스트 |
| 6 | 이용약관 | `/terms` | `TermsOfServicePage` | 이홍연 | 이용약관 정적 텍스트 |
| 7 | 위시 | `/wish` | `WishPage` | 이홍연 | 위시 등록 상품을 유형별(전체/받고 싶은/주고 싶은)로 조회 |
| 8 | 위시 등록 | `/wish/create` | `WishCreatePage` | 이홍연 | 유형·이름·가격·구매처·이미지 입력 후 위시 등록 |
| 9 | 위시 검색 | `/wish/search` | `WishSearchPage` | 이홍연 | 등록된 위시 검색 + 최근 검색어 |
| 10 | 위시 수정 | `/wish/:id/edit` | `WishEditPage` | 이홍연 | 기존 위시 유형·이름·가격·구매처·이미지 수정 |
| 11 | 마이페이지 | `/my` | `MyPage` | 이홍연 | 프로필 요약 + 메뉴 리스트 (비로그인/로그인/관리자 3분기) |
| 12 | 내 정보 수정 | `/my/profile` | `ProfileEditPage` | 이홍연 | 닉네임/사진 변경, 로그아웃, 계정 삭제 |
| 13 | 등록 계좌 목록 | `/my/accounts` | `AccountListPage` | 이홍연 | 등록된 나의 계좌 목록 조회 |
| 14 | 계좌 등록 | `/my/accounts/new` | `AccountFormPage` | 이홍연 | 은행 선택(아이콘 그리드) + 계좌번호 자동추천 + 등록 |
| 15 | 계좌 수정 | `/my/accounts/:id/edit` | `AccountFormPage` | 이홍연 | 기존 계좌 정보 수정 |
| 16 | [관리자] 상품 목록 | `/admin/products` | `AdminProductsPage` | 이홍연 | 홈 "선물 둘러보기" 노출 상품 목록/카테고리 관리, 선택 삭제 |
| 17 | [관리자] 상품 등록 | `/admin/products/new` | `AdminProductFormPage` | 이홍연 | 신규 상품 등록 |
| 18 | [관리자] 상품 수정 | `/admin/products/:productId/edit` | `AdminProductFormPage` | 이홍연 | 기존 상품 수정 |
| 19 | [관리자] 초대장 테마 관리 | `/admin/invitation-themes` | `AdminInvitationThemesPage` | 이홍연 | 초대장 배경 색상/캐릭터 등록·수정·삭제 |
| 20 | 내 선물 목록 | `/my/fundings/my` | `MyGiftListPage` | 우준영 | 내가 개설한 "내 선물" 펀딩 목록 |
| 21 | 함께 선물 목록 | `/my/fundings/together` | `TogetherGiftListPage` | 우준영 | 내가 참여 중인 "함께 선물" 목록 |
| 22 | 선물 페이지 이용 방법 | `/gift/about` | `GiftAboutPage` | 우준영 | 내 선물/함께 선물 탭 안내 + 하단 CTA로 만들기 진입 |
| 23 | 내 선물 만들기 (5단계) | `/gift/create/my` | `FundingCreatePage` | 최수민 | 기본정보→위시→공개범위→계좌→초대장의 5단계 개설 플로우 + 완료 화면 |
| 24 | 함께 선물 만들기 (3단계) | `/gift/create/together` | `GiftCreateTogetherPage` | 최수민 | 기본정보→계좌→초대장의 3단계 개설 플로우 + 완료 화면 |
| 25 | 펀딩 초대장 | `/funding/:id/invitation` | `InvitationPage` | 장하영 | 개설자가 커스텀한 초대장 조회, '축하하러 가기' 진입 |
| 26 | 펀딩 상세 조회 | `/funding/:id` | `FundingDetailPage` | 장하영 | 펀딩 진행률·위시·메세지 조회 (개설자/참여자 뷰 분기) |
| 27 | 펀딩 수정 (선택) | `/funding/:id/edit` | `FundingEditSelectPage` | 장하영 | 수정할 항목 선택 |
| 28 | 펀딩 수정 (단계) | `/funding/:id/edit/:step` | `FundingEditStepPage` | 장하영 | 선택한 항목 단계별 수정 |
| 29 | 축하 메세지 전체보기 | `/funding/:id/messages` | `MessagesPage` | 장하영 | 축하 메세지 전체 조회 |
| 30 | 펀딩 참여 (4단계) | `/funding/:id/participate` | `ParticipatePage` | 장하영 | 이름→편지→금액→입금의 4단계 참여 흐름 |
| 31 | 참여 완료 | `/funding/:id/complete` | `CompletePage` | 장하영 | 참여 완료 후 축하 화면 표시 |
| 32 | 함께 선물 메인 | `/group/:id` | `GroupPage` | 장하영 | 함께 선물 대시보드 — 진행 상태, 참여자, 후보 요약, 상태별 액션 |
| 33 | 선물 후보 목록/투표 | `/group/:id/candidates` | `CandidatesPage` | 장하영 | 선물 후보 목록 조회 및 투표 (최대 3표, 비로그인 조회만 가능) |
| 34 | 선물 후보 상세 | `/group/:id/candidates/:candidateId` | `CandidateDetailPage` | 장하영 | 후보 상세 정보 + 투표 |
| 35 | 선물 후보 의견 | `/group/:id/candidates/:candidateId/comments` | `CandidateCommentsPage` | 장하영 | 후보에 대한 의견(댓글) 목록/작성 |
| 36 | 선물 후보 등록 | `/group/:id/candidates/new` | `CandidateNewPage` | 장하영 | 새 선물 후보 등록 (공동관리자 이상) |
| 37 | 참여자 더보기 (조회) | `/group/:id/participants` | `ParticipantViewPage` | 장하영 | 참여자 목록 조회 전용 |
| 38 | 참여자 관리 | `/group/:id/participants/manage` | `ParticipantsPage` | 장하영 | 참여자 역할 변경 (개설자·공동관리자) |
| 39 | 편지 남기기 | `/group/:id/letter` | `LetterPage` | 장하영 | 정산 시 함께 남기는 편지 작성 (색상 선택 포함) |
| 40 | 정산하기 (참여자) | `/group/:id/settle` | `SettlePage` | 장하영 | 계좌 조회 및 입금 완료 신고 |
| 41 | 정산하기 (개설자) | `/group/:id/settle/host` | `HostSettlePage` | 장하영 | 참여자별 정산 상태 확인 및 변경 |
| 42 | 선물 확정 | `/group/:id/confirm` | `ConfirmPage` | 장하영 | 후보 중 최종 선물 확정 (4단계) |
| 43 | 선물 목록 수정 | `/group/:id/confirm/edit` | `ConfirmEditPage` | 장하영 | 확정된 선물 목록 수정 (개설자 전용) |
| 44 | 선물 페이지 수정 (메뉴) | `/group/:id/edit` | `GroupEditPage` | 장하영 | 수정할 항목 선택 (개설자 전용) |
| 45 | 기본 정보 수정 | `/group/:id/edit/basic` | `GroupEditBasicPage` | 장하영 | 방 이름 등 기본 정보 수정 |
| 46 | 계좌 정보 수정 | `/group/:id/edit/account` | `GroupEditAccountPage` | 장하영 | 정산 계좌 수정 |
| 47 | 초대장 수정 | `/group/:id/edit/invitation` | `GroupEditInvitationPage` | 장하영 | 초대장 색상/캐릭터 수정 |
| 48 | 구매 인증 업로드 | `/group/:id/purchase-upload` | `PurchaseUploadPage` | 장하영 | 실제 구매 완료 인증(영수증/링크) 업로드 |
| 49 | 함께 선물 메세지 전체보기 | `/group/:id/messages` | `GroupMessagesPage` | 장하영 | 축하 메세지 봉투 그리드, 탭하면 편지 팝업 |
| 50 | 선물 후기 내용 작성 | `/gift/review/write/:type/:fundingId?` | `ReviewContentWritePage` | 우준영 | 3유형(선물 후기/소식 전하기/마음 전하기) 제목·내용·이미지 작성 |
| 51 | 선물 후기 초대장 꾸미기 | `/gift/review/write/:type/:fundingId?/invitation` | `ReviewWritePage` | 우준영 | 후기 내용 작성 다음 단계 — 초대장 색상·캐릭터 선택 |
| 52 | 선물 후기 작성 완료 | `/gift/review/complete/:type/:fundingId?` | `ReviewCompletePage` | 우준영 | 작성 완료 화면 — 링크 복사·공유·미리보기 |
| 53 | 후기 초대장 미리보기 | `/gift/review/complete/:type/:fundingId/invitation-preview` | `ReviewInvitationPreviewPage` | 우준영 | 완료 화면에서 초대장 미리보기 확대 |
| 54 | 선물 후기 조회 | `/gift/review/:id/:fundingId?` | `GiftReviewDetailPage` | 우준영 | 이미지 캐러셀 + 편지지 본문 조회 (배경색 반영) |

> **바텀시트·모달 등 라우트 없는 하위 UI** (부모 페이지 상태로 노출):
> `TermsBottomSheet`(약관), `PriceFilterSheet`·`WishTypeSheet`·`GiftCreateSheet`·`WishCreateSheet`(홈/위시/선물 만들기 진입), `WishEditModeSheet`(위시 카드 "⋮"), `PhotoActionSheet`·`ImageCropper`(이미지 등록/자르기, 프로필·위시·선물·후기 공용), `BankSelectSheet`(은행 선택), `DateSheet`(날짜 선택), `EmojiPopup`(삭제 확인 등 범용 알림), `ConfirmModal`(로그아웃·삭제·이탈 확인).

---

## 3. 라우팅 구조

`src/main.tsx`에서 `QueryClientProvider` → `BrowserRouter` → `AuthProvider` 순으로 앱을 감싸고, `src/App.tsx`에서 `Routes`/`Route`로 경로를 정의합니다. 라우트 트리 최상단에 `PageViewTracker`를 함께 마운트해 라우트 변경마다 GA4 `page_view`를 전송합니다.

```tsx
// src/main.tsx
initKakao(KAKAO_JS_KEY)
initGA()
// ...
<QueryClientProvider client={queryClient}>
  <BrowserRouter>
    <AuthProvider>
      <App />
    </AuthProvider>
  </BrowserRouter>
</QueryClientProvider>
```

```tsx
// src/App.tsx (구조 발췌 — 전체 54개 라우트는 2번 표 참고)
<PageViewTracker />
<Routes>
  <Route path="/" element={<SplashPage />} />
  <Route path="/login" element={<LoginPage />} />
  <Route path="/home" element={<HomePage />} />
  {/* 위시, 마이, 관리자(/admin/*) */}
  {/* 내 선물: /gift/create/my, /funding/:id/* */}
  {/* 함께 선물: /gift/create/together, /group/:id/* (H섹션) */}
  {/* 선물 후기: /gift/review/* */}
</Routes>
```

> 라우팅 방식: `BrowserRouter`(History API) 기반. 동적 파라미터는 `/funding/:id`, `/group/:id`, `/gift/review/:id`, `/gift/review/write/:type` 등에서 사용하며, `:fundingId?` 처럼 선택적 파라미터도 활용합니다.
> SPA 새로고침 대응을 위해 배포 환경(Vercel)에 SPA fallback(`vercel.json` rewrite) 설정이 되어 있습니다.

---

## 4. 화면별 진입 경로

| 화면 | 진입 경로 (트리거) |
| --- | --- |
| 스플래시 `/` | 앱 최초 진입 (URL 루트) |
| 홈 `/home` | 스플래시 4초 후 자동 이동(`replace`), 하단 네비게이션 "홈" 탭 |
| 로그인 `/login` | 홈 배너의 "로그인" 버튼 (비로그인 상태에서만 노출) |
| 프로필 설정 `/signup/profile` | 로그인에서 소셜 로그인 후 신규 가입자일 때 |
| 위시 `/wish` | 하단 네비게이션 "위시" 탭 |
| 위시 등록 `/wish/create` | 위시 화면 등록 버튼 |
| 위시 검색 `/wish/search` | 위시 화면 검색 아이콘 |
| 위시 수정 `/wish/:id/edit` | 위시 카드 "⋮" 액션 시트(`WishEditModeSheet`)에서 "수정하기" |
| 선물 만들기 진입 | 홈 배너 "선물 페이지 만들기" 또는 `/gift/about` 하단 CTA → `GiftCreateSheet` 바텀시트 → `/gift/create/my`(내 선물) / `/gift/create/together`(함께 선물) |
| 이용 방법 `/gift/about` | 홈/진입 시트의 "이용 방법" 안내 링크 |
| 마이페이지 `/my` | 하단 네비게이션 "마이" 탭 |
| 내 선물·함께 선물 목록 `/my/fundings/*` | 마이페이지 메뉴 |
| 관리자 메뉴 `/admin/*` | 마이페이지 — 관리자 계정으로 로그인 시에만 메뉴 노출(`useRequireAdmin` 가드) |
| 계좌 목록 `/my/accounts` | 마이페이지 메뉴 "등록된 나의 계좌" |
| 계좌 등록/수정 `/my/accounts/new`·`/:id/edit` | 계좌 목록에서 "새로운 계좌 등록하기" / 개별 계좌 수정 |
| 펀딩 초대장 `/funding/:id/invitation` | 공유받은 초대장 링크 |
| 펀딩 상세 `/funding/:id` | 초대장 "축하하러 가기", 내 선물 목록에서 카드 클릭 |
| 펀딩 참여 `/funding/:id/participate` | 펀딩 상세에서 "축하하러 가기" |
| 함께 선물 메인 `/group/:id` | 초대장(함께 선물) 링크, 함께 선물 목록에서 카드 클릭 |
| 선물 후보/투표·정산·확정 등 | 함께 선물 메인의 진행 상태별 CTA |
| 선물 후기 작성 `/gift/review/write/:type/:fundingId?` | 내 선물 참여·함께 선물 완료 화면의 후기/소식/마음 전하기 진입 |
| 후기 작성 완료 `/gift/review/complete/:type/:fundingId?` | 후기 초대장 꾸미기 화면 "저장" |
| 후기 조회 `/gift/review/:id/:fundingId?` | 완료 화면 "미리보기" / 공유 링크 |

---

## 5. 페이지별 담당자

> `git log` 커밋 작성자 기준. 동일인이 여러 GitHub 계정명으로 커밋: `HongYeonLee`/`Hongyeon Lee` = 이홍연, `snow-jun-0`/`Jun` = 우준영, `jhy335501`/`Hayeong` = 장하영. (백엔드 담당자가 초기 위시 기능 구현을 일시적으로 도운 이력이 있으나, 이후 이홍연이 전면 수정하여 현재는 이홍연 담당 영역입니다.)

| 담당자 | 담당 영역 | 대표 화면·모듈 |
| --- | --- | --- |
| 이홍연 | 온보딩(A/B)·홈·위시(F)·마이(I)·계좌·관리자·약관 + 공통 인프라 | 스플래시/로그인/프로필설정/홈/위시/위시등록/위시검색/마이/내정보수정/계좌/관리자(상품·초대장테마)/약관, 공통 컴포넌트, API·인증·분석 인프라(`api/`, `lib/`) |
| 최수민 | 선물 만들기 플로우(D/G) | `FundingCreatePage`, `GiftCreateTogetherPage`, `components/create/Step*`·`TogetherStep*`, `store/fundingCreateStore`·`togetherCreateStore` |
| 우준영 | 선물 만들기 진입(C)·선물 후기(J)·내 선물/함께 선물 목록 | `gift-about`, `gift-create/GiftCreateSheet`, `gift-review`(작성/완료/조회), `my/MyGiftListPage`·`TogetherGiftListPage` |
| 장하영 | 초대장(E01)·펀딩 상세/참여/수정(E)·함께 선물 참여 전체(H) | `invitation`, `funding`(상세/수정/메세지), `participate`, `group/`(대시보드·후보·투표·정산·확정·편지·참여자 관리) |

---

## 6. 공통 컴포넌트 목록 및 사용 방식

`src/components/common` — 페이지 전반에서 재사용되는 UI 컴포넌트입니다. (실제 파일 기준 교차 검증 완료)

| 컴포넌트 | 설명 |
| --- | --- |
| `Button` | 하단 CTA 버튼 (h52 / radius12, `disabled` 비활성) |
| `TextField` | 라벨 + 글자수 카운터 입력창 (한글 IME 초과 입력 자동 컷) |
| `Header` | 상단 네비게이션 바 (뒤로가기 + 타이틀 + 우측 슬롯) |
| `BottomSheet` | 하단에서 올라오는 시트 (딤 + 그래버, 드래그 지원) |
| `BottomNav` | 하단 탭 네비게이션 + 플로팅(+) 버튼 |
| `MenuRow` | 메뉴 리스트 행 (우측 셰브런 옵션) |
| `ConfirmModal` | 확인 모달 (2버튼) — 로그아웃·삭제·이탈 확인 |
| `EmojiPopup` | 이모티콘(느낌표/체크) + 제목/설명 + 버튼 1~2개 범용 팝업 |
| `Toast` | 하단 스낵바 토스트 |
| `LetterCard` / `LetterColorPicker` / `letterPalette.ts` | 편지지 카드·색상 선택 (BE `hexCode` 기반 파생, E·H·J 공용) |
| `DefaultAvatar` | 기본 프로필 아바타 |
| `AccountCard` / `AccountFormFields` / `AccountConfirmModal` / `BankSelectSheet` | 계좌 등록/수정 세트 (마이·D·G 공용, 계좌번호 자동 은행 추천 포함) |
| `CategoryChips` | 카테고리 필터/선택 칩 목록 (홈 스타일, 관리자 화면 등에서 재사용) |
| `SelectCheckBadge` / `SelectModeBar` | 선택 모드 체크 배지·하단 삭제 바 (관리자 상품/초대장 테마 관리) |
| `CheckOption` | 체크박스 + 라벨 |
| `PhotoActionSheet` / `ImageCropper` / `WebImageSearch` | 이미지 업로드 바텀시트 → 크롭/웹 검색까지 이어지는 흐름 (프로필·위시·선물·후기 공용) |
| `ProcessBar` | 상단 단계 인디케이터 |
| `StickyBottomBar` | 하단 고정 CTA 래퍼 |
| `PageViewTracker` | 라우트 변경마다 GA4 `page_view` 전송 (렌더링 없음) |

**아이콘 컴포넌트** (`src/components/icons`) — SVG를 React 컴포넌트로 래핑, `className`으로 색상/크기 제어. Home/Gift/Profile(Bold 쌍 포함)/Plus/Search/Check/Close/CaretDown/ChevronLeft·Right/Star/Wallet/Bank/Chat/Message/Link/Share/Lock/LogOut/Expand/ZoomIn/ZoomOut/Edit/More/CircleCheck/ArrowUpRight/Trash/BadgeTail 등. (일부 화면은 `lucide-react` 아이콘 병용)

**초대장 컴포넌트** (`src/components/invitation`) — `InvitationVisual`(드롭인, fundingId 조회 모드/미리보기 모드 지원), `InvitationHero`, `inviteTheme.ts`(고정 팔레트), `characterAssets.ts`. E01·J 화면 공용.

**생성 플로우 컴포넌트** (`src/components/create`, 최수민) — 내 선물(Step1~5)·함께 선물(TogetherStep1~3) 개설 폼, `Mascot.tsx`(초대장 색상 파생 공용 함수 `getInvitePreviewAccent`/`getInvitationCompletionColors`/`InviteColorSwatchGrid`), `InvitationPreviewCard`, `DateSheet`, `StepIndicator`.

> 🎨 디자인 토큰: 색상/타이포는 `src/index.css`의 Tailwind `@theme`에 정의. 하드코딩 대신 `text-gray-900`, `bg-pink-500`, `text-h3-sb`, `text-b2-m` 등 토큰 클래스를 사용합니다.

---

## 7. 데이터 관리 (API 연동 현황)

`src/api/` 아래 16개 모듈(`auth` `users` `userAccounts` `wishlists` `fundings` `groupFundings` `contributions` `reviews` `decorations` `individualDraft` `togetherDraft` `invitationThemes` `products` `metaApi` `images` `webImages`) 전부 `apiClient`(axios) 기반의 **실제 백엔드 연동**입니다.

| 도메인 | 상태 | 위치 |
| --- | --- | --- |
| 소셜 로그인(카카오/구글)·로그아웃·토큰 자동 갱신 | ✅ 연동 | `api/auth.ts`, `lib/apiClient.ts` |
| 프로필 조회/수정/탈퇴·계좌 CRUD | ✅ 연동 | `api/users.ts`, `api/userAccounts.ts` |
| 위시 CRUD·검색 | ✅ 연동 | `api/wishlists.ts` |
| 내 선물·함께 선물 페이지 생성/수정/임시저장 | ✅ 연동 | `api/fundings.ts`, `api/individualDraft.ts`, `api/togetherDraft.ts` |
| 함께 선물 대시보드·후보 조회/투표/등록·정산 조회/입금 신고/상태 변경·참여자 역할 변경 | ✅ 연동 | `api/groupFundings.ts`, `api/contributions.ts` |
| 초대장 테마·캐릭터 (조회 + 관리자 CRUD) | ✅ 연동 | `api/invitationThemes.ts`, `api/metaApi.ts` |
| 선물 후기 작성/조회 | ✅ 연동 | `api/reviews.ts`, `api/decorations.ts` |
| 관리자 상품 관리 | ✅ 연동 | `api/products.ts` |
| 이미지 업로드(S3 presigned URL) / 웹 이미지 검색 | ✅ 연동 | `api/images.ts`, `api/webImages.ts` |
| GA4(Google Analytics) 이벤트 트래킹 | ✅ 연동 | `lib/analytics.ts` (`VITE_GA_MEASUREMENT_ID` 미설정 시 비활성) |

**API 공통 규약** — 모든 응답은 `ApiEnvelope<T>` 형태(`{ isSuccess, code, message, result }`)이며, `unwrap()` 헬퍼가 `result`만 반환하고 실패 시 `ApiError`를 던집니다.
백엔드 베이스 URL: `import.meta.env.VITE_API_BASE_URL`(기본값 `https://dev.api.toget.kr`, `src/lib/apiClient.ts` 참고).
Swagger 문서: [dev.api.toget.kr/swagger-ui](https://dev.api.toget.kr/swagger-ui/index.html).

---

## 8. 상태 관리 방식

| 범위 | 방식 | 구현 |
| --- | --- | --- |
| 인증(로그인) 상태 | **Context API** | `contexts/AuthProvider.tsx` — `isLoggedIn`, `login()`, `logout()`. `hooks/useAuth.ts`로 소비. `apiClient`의 `onAuthExpired`를 구독해 강제 로그아웃 처리 |
| 서버 상태 | **TanStack Query** | `lib/queryClient.ts` + `QueryClientProvider`. `useMyProfile`, `useUserAccounts` 등 `useQuery` 훅 |
| 클라이언트 전역 상태 | **Zustand** | `store/fundingCreateStore.ts`(내 선물 5단계 입력·수정 스냅샷), `store/togetherCreateStore.ts`(함께 선물 입력값), `store/wishStore.ts`(위시 토글) |
| 지역(UI) 상태 | **useState** | 시트/모달 열림, 스텝 인덱스, 필터 선택, 캐러셀 인덱스 등 |
| 화면 간 데이터 전달 | **Router state** | 후기 작성→완료→조회 데이터 전달, 마이페이지 토스트 메시지 등 |
| 토큰 저장 | **localStorage** | `lib/tokenStorage.ts` (`toget_access_token` / `toget_refresh_token`) |
| 관리자 판별 | **이메일 화이트리스트** | `lib/admin.ts` — UI 노출용, 실제 권한 검증은 매 요청 백엔드가 재수행 |

> `AuthProvider`의 로그인 초기값은 `localStorage`에 access token이 있는지로 판단합니다(`getAccessToken() !== null`). 로그아웃/탈퇴 시 `clearTokens()`로 토큰을 지우기 때문에 새로고침해도 로그인 상태가 정확히 반영됩니다.

---

## 9. 트러블 슈팅 기록

> 노션 [트러블 슈팅 목록](https://app.notion.com/p/ef9ac56ca078822da919011f40ed5bf4) 기준 실제 팀 기록 사례입니다. 아래는 각 발생 시점 기준 과거 기록이며, 이후 관련 영역이 리팩터링되었더라도 문제 자체와 해결 과정은 유효한 이력이라 그대로 유지합니다.

### TS-01. 한글 입력 시 글자수 제한(maxLength) 초과 입력

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | `TextField`에 `maxLength`를 지정해도 한글 조합(IME) 입력 중에는 브라우저가 제한을 강제하지 않아 최대 글자수를 초과 입력됨 |
| 발생 시점 | 프로필 설정 / 내 정보 수정 닉네임 입력 |
| 담당자 | 이홍연 |
| 원인 | IME 조합(composition) 중에는 `maxLength` 속성이 적용되지 않는 브라우저 동작 |
| 해결 방법 | `onChange` 핸들러에서 `value.length > maxLength`인 경우 `slice(0, maxLength)`로 직접 잘라냄 |
| 참고 자료 | `components/common/TextField.tsx` |

### TS-02. 토스트가 새로고침 시 다시 노출되는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 로그아웃/계정삭제 후 마이페이지로 이동하며 토스트를 띄우는데, 새로고침 시 토스트가 재노출될 수 있음 |
| 발생 시점 | 내 정보 수정 → 로그아웃/계정삭제 완료 이동 |
| 담당자 | 이홍연 |
| 원인 | 토스트 메시지를 `navigate` state로 전달하는데, state가 히스토리에 남아 재사용됨 |
| 해결 방법 | 마운트 시 메시지를 읽은 뒤 `navigate(pathname, { replace: true, state: null })`로 state를 즉시 소거하고 일정 시간 후 숨김 처리 |
| 참고 자료 | `pages/my/MyPage.tsx` |

### TS-03. 머지 충돌 해결 실수로 코드 중복·손상 발생

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 여러 PR을 dev에 머지하는 과정에서 코드가 중복 삽입되어 하단 탭바가 2개 렌더링되고 dev 브랜치 빌드가 실패함 |
| 발생 시점 | 다수 PR dev 머지 시점 |
| 담당자 | 우준영 |
| 원인 | 충돌 해결 시 `Accept both changes`를 눌러 양쪽 변경분이 통째로 삽입됨. 머지 이후 dev 빌드를 확인하는 담당이 없어 깨진 채 방치됨 |
| 해결 방법 | hotfix 브랜치로 즉시 복구. 이후 `Accept both changes` 사용 금지(수동 병합), 머지 후 dev 빌드 확인을 루틴화 |
| 참고 자료 | `App.tsx`, `pages/gift-about/GiftAboutPage.tsx`, `pages/home/HomePage.tsx` |

### TS-04. Approved된 PR이 머지되지 않고 누적되는 병목

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | Open PR 10건이 전부 `status:review` 상태로 쌓이고, 리뷰어 Approve를 받은 PR도 5일간 머지되지 않아 후속 작업이 막힘 |
| 발생 시점 | 2026.07.19 |
| 담당자 | 우준영 (팀장) |
| 원인 | 리뷰어 승인 후에도 별도 머지 담당이 없어 대기만 하는 구조. 프로세스가 흐르지 않고 정체됨 |
| 해결 방법 | 팀 합의로 작성자 본인이 승인된 PR을 직접 머지할 수 있도록 규칙 완화 |
| 참고 자료 | GitHub `UMC-toget/ToGet-FE` PR 목록 |

### TS-05. 컨벤션 문서와 실제 레포 구현의 불일치

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 컨벤션 문서에는 `features/도메인/` 구조가 명시돼 있으나, 실제 레포는 `src/pages/<도메인>/` + `src/components/common/` 구조로 되어 있어 신규 팀원이 혼란을 겪음 |
| 발생 시점 | 1차 과제 피드백 반영 시점 |
| 담당자 | 우준영 (팀장) |
| 원인 | 초기 컨벤션 문서가 BE 기준(PayLinker 기반)으로 작성됨. 문서를 먼저 쓰고 코드가 따라가지 못해 괴리 발생 |
| 해결 방법 | 컨벤션 문서를 실제 FE 레포 기준으로 전면 재작성, 공용 컴포넌트 관리표 최신화 |
| 참고 자료 | 프론트 코딩 컨벤션 문서 (노션) |

### TS-06. 디자인 위치 오인으로 캐릭터 선택 UI를 재구현

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | J파트 후기 작성 화면의 캐릭터 선택 UI를 구현하는데, 피그마에 해당 디자인이 없는 줄 알고 E파트(Step5Invite) 패턴을 가져와 임의 구현했다가, 이후 실제 디자인을 발견해 다시 맞춰야 했음 |
| 발생 시점 | 이슈 #103 작업 중 |
| 담당자 | 우준영 |
| 원인 | 'J01-1) 후기: 초대장 만들기' 프레임에 실제 디자인이 있었으나 초기에 찾지 못함 |
| 해결 방법 | 실제 프레임 확인 후 재구현 |
| 참고 자료 | 이슈 #103, 피그마 'J01-1' 프레임 |

### TS-07. blob URL이 조회 화면에서 깨지는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 선물 후기 작성 → 완료 → 조회로 화면을 넘길 때 등록한 이미지가 조회 화면에서 표시되지 않음 |
| 발생 시점 | 이슈 #119 (후기 조회 화면 데이터 반영) 작업 중 |
| 담당자 | 우준영 |
| 원인 | 화면 간 이미지를 `navigate` state로 전달하는데, blob URL이 페이지 전환 후 revoke되어 무효화됨 |
| 해결 방법 | 이미지를 base64 data URL로 변환해 state에 담아 전달 |
| 참고 자료 | 이슈 #119, `pages/gift-review/` |

### TS-08. 히어로 이미지 비율이 깨지는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 후기 작성 완료 화면의 상단 히어로 영역을 개별 요소로 조립했더니 화면에서 비율이 맞지 않고 깨짐 |
| 발생 시점 | 이슈 #85 (후기 작성 완료 화면) 작업 중 |
| 담당자 | 우준영 |
| 원인 | 여러 레이어를 코드로 개별 조립하면서 피그마 원본 비율을 정확히 재현하지 못함 |
| 해결 방법 | 조립 대신 피그마에서 통짜 이미지로 export(`review-complete-hero.png`, 2x)해 교체 |
| 참고 자료 | 이슈 #85, `review-complete-hero.png` |

### TS-09. 카카오톡 소셜 로그인 Redirect URL, KOE010 에러 해결하기

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 카카오 소셜 로그인 시도 시 네트워크 응답에 "Bad client credentials"와 함께 `KOE010` 에러 코드 반환 |
| 발생 시점 | 카카오 로그인 클라이언트 SDK 연동 |
| 담당자 | 이홍연 |
| 원인 | 카카오 디벨로퍼스 앱의 Client Secret이 활성화돼 있었는데 클라이언트 요청에서 이 값을 전송하지 않아, 토큰 요청 시 필수 파라미터가 누락됨 |
| 해결 방법 | 카카오 디벨로퍼스에서 REST API Key·JavaScript 키 모두에 로그인 Redirect URL 등록, Client Secret의 카카오 로그인 토글을 꺼서 인증 없이 진행되도록 변경 |
| 참고 자료 | [카카오 소셜 로그인 KOE010 에러 해결기](https://hongyeonlee.tistory.com/109) |

### TS-10. 로그아웃/탈퇴 후 새로고침하면 로그인 상태로 돌아가는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 로그아웃하거나 계정을 삭제한 뒤 마이페이지에서 새로고침하면 다시 로그인 상태로 표시됨 |
| 발생 시점 | dev 통합 후 실제 로그인 API 연동 테스트 중 |
| 담당자 | 이홍연 |
| 원인 | `AuthProvider`의 `isLoggedIn` 초기값이 실제 토큰 존재 여부와 무관하게 `useState(true)`로 하드코딩되어 있었음 |
| 해결 방법 | 초기값을 `getAccessToken() !== null`로 변경 |
| 참고 자료 | `contexts/AuthProvider.tsx` |

### TS-11. 회원가입 시 등록한 프로필 사진이 마이페이지에 반영되지 않는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 회원가입 마지막 단계(닉네임/사진 설정)에서 사진을 등록해도, 이후 마이페이지에서 확인하면 항상 기본 아바타로 표시됨 |
| 발생 시점 | dev 통합 후 실제 로그인 API 연동 테스트 중 |
| 담당자 | 이홍연 |
| 원인 | `ProfileSetupPage`의 `ProfileAvatar`에 `onSelect`가 연결되어 있지 않아, 크롭까지 완료한 사진 File이 애초에 어디로도 전달되지 않았음 |
| 해결 방법 | `onSelect`로 받은 File을 "가입" 확정 시 `uploadImage()`(presigned URL 업로드)로 먼저 올리고, 그 `imageUrl`을 닉네임과 함께 `updateMyProfile`에 포함해 저장 |
| 참고 자료 | `pages/signup/ProfileSetupPage.tsx`, `api/images.ts` |

### TS-12. 프로덕션 도메인에서 카카오 로그인 시 Vercel 404 발생

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 로컬(Vite dev 서버)에서는 카카오 로그인이 정상 동작하지만, www.toget.kr / Vercel 배포 도메인에서는 인증 완료 후 리다이렉트되는 순간 Vercel의 `404: NOT_FOUND` 에러 페이지가 뜸 |
| 발생 시점 | 프로덕션 도메인 통합 테스트 중 |
| 담당자 | 이홍연 |
| 원인 | `BrowserRouter` 사용 중 카카오 OAuth가 인증 후 `/login?code=...`로 풀 페이지 리다이렉트를 발생시키는데, SPA 폴백 설정이 없으면 정적 호스팅인 Vercel이 자체 404를 반환함 |
| 해결 방법 | 저장소 루트에 `vercel.json`을 추가해 모든 경로를 `index.html`로 rewrite |
| 참고 자료 | `vercel.json` |

---

## 10. 배포

| 항목 | 내용 |
| --- | --- |
| 배포 플랫폼 | Vercel |
| 배포 방식 | Vercel 무료 플랜은 GitHub **조직(Organization)** 저장소 연동 배포를 지원하지 않아, 개인 저장소(`HongYeonLee/ToGet-FE`)를 미러로 두고 그쪽을 Vercel과 연동해 우회 |
| 자동화 | `.github/workflows/deploy.yml` — `main` push 시 저장소 전체를 미러 저장소로 push, Vercel이 그 push를 감지해 프로덕션 배포 실행. `preview.yaml` — `main`/`dev` 대상 PR Preview 배포 |
| 환경 변수 | `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`(Secrets), `VITE_API_BASE_URL`(백엔드 주소), `VITE_GA_MEASUREMENT_ID`(GA4, 선택) |
| **배포 주소** | [`https://www.toget.kr`](https://www.toget.kr) (`to-get-fe.vercel.app` 등 Vercel 기본 도메인으로도 접근 가능) |

**배포 확인 체크리스트**

- [ ] 배포 링크 외부 접속 가능
- [ ] 주요 화면 정상 렌더링 (스플래시/홈/로그인/위시/마이/선물 만들기/함께 선물/후기)
- [ ] 라우팅·새로고침 정상 작동 (SPA fallback — `/funding/1` 등 새로고침 시 404 여부)
- [ ] 모바일/태블릿/데스크톱 레이아웃 확인 (max-width 402px 중심)
- [ ] API 호출 정상 (`https://dev.api.toget.kr`)
- [ ] 콘솔 치명적 에러 없음
