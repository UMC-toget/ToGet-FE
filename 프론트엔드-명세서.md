# 📄 ToGet Frontend 명세서

> 본 문서는 **`dev` 브랜치**의 실제 소스코드를 기준으로 작성되었습니다. (GitHub 교차 검증 완료)
> 저장소: [UMC-toget/ToGet-FE](https://github.com/UMC-toget/ToGet-FE) · 기준 브랜치: `dev`

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | ToGet (선물 펀딩 서비스) |
| 플랫폼 | 모바일 우선 반응형 웹 (max-width 402px) |
| 프레임워크 | React 19 + TypeScript + Vite |
| 스타일 | Tailwind CSS v4 (`@theme` 디자인 토큰) |
| 라우팅 | react-router-dom v7 |
| 상태 관리 | Context API(인증) + Zustand(클라이언트) + TanStack Query(서버) |
| 데이터 | 일부 API 연동 (프로필·계좌) + 나머지 Mock |

---

## 1. 프로젝트 소개

ToGet은 생일·졸업·집들이 등의 선물을 여러 사람이 함께 준비하는 **선물 펀딩 서비스**입니다.
사용자는 받고 싶은 선물을 모아 선물 페이지(펀딩)를 만들고, 초대장을 공유해 친구들과 함께 금액을 모아 선물을 준비하며, 선물이 전달된 뒤에는 후기·감사 인사를 남길 수 있습니다.

이번 단계에서는 **온보딩 → 홈/위시 → 선물 만들기 → 펀딩 참여 → 마이(계좌 관리) → 선물 후기**에 이르는 전체 서비스 흐름의 화면을 구현했습니다.
프로필·계좌 등 일부 도메인은 실제 백엔드 API와 연동되며, 아직 연동되지 않은 영역은 Mock 데이터로 렌더링하고 코드 내 `// TODO` 주석으로 연동 지점을 표기했습니다.

---

## 2. 전체 화면 목록

`dev` 브랜치 `App.tsx`에 등록된 라우팅 기준 전체 화면입니다. (담당자는 `git log` 커밋 작성자 기준)

| No | 화면명 | Route Path | Page Component | 담당자 | 주요 기능 |
| --- | --- | --- | --- | --- | --- |
| 1 | 스플래시 | `/` | `SplashPage` | 이홍연 | 진입 로고 노출, 2초 후 로그인으로 자동 이동 |
| 2 | 로그인 | `/login` | `LoginPage` | 이홍연 | 카카오/구글 소셜 로그인 진입 |
| 3 | 프로필 설정 | `/signup/profile` | `ProfileSetupPage` | 이홍연 | 최초 가입자 닉네임·프로필 설정, 약관 동의 후 가입 |
| 4 | 홈 | `/home` | `HomePage` | 이홍연 | 배너 + 선물 둘러보기(카테고리/가격 필터) + 내 펀딩 목록 |
| 5 | 위시 | `/wish` | `WishPage` | 이홍연 | 위시 등록 상품을 유형별(전체/받고 싶은/주고 싶은)로 조회, 카드 클릭 시 구매처 외부 링크 이동 |
| 6 | 위시 수정하기 | `/wish/:id/edit` | `WishEditPage` | 이홍연 | 위시 카드 "⋮" 액션 시트에서 "수정하기" 선택 시 진입, 유형·이름·가격·구매처·이미지 수정 |
| 7 | 마이페이지 | `/my` | `MyPage` | 이홍연 | 프로필 요약 + 메뉴 리스트 (로그인 여부 분기) |
| 8 | 내 정보 수정 | `/my/profile` | `ProfileEditPage` | 이홍연 | 닉네임/사진 변경, 로그아웃, 계정 삭제 |
| 9 | 등록 계좌 목록 | `/my/accounts` | `AccountListPage` | 이홍연 | 등록된 나의 계좌 목록 조회 (API 연동) |
| 10 | 계좌 등록 | `/my/accounts/new` | `AccountFormPage` | 이홍연 | 은행 선택 + 계좌 정보 입력·등록 (API 연동) |
| 11 | 계좌 수정 | `/my/accounts/:id/edit` | `AccountFormPage` | 이홍연 | 기존 계좌 정보 수정 (API 연동) |
| 12 | 선물 만들기 (5단계) | `/funding/create` | `FundingCreatePage` | 최수민 | 기본정보→위시→공개범위→계좌→초대의 5단계 만들기 플로우 + 완료 |
| 13 | 선물 페이지 이용 방법 | `/gift/about` | `GiftAboutPage` | 우준영 | 내 선물/함께 선물 탭 안내 + 하단 CTA로 만들기 진입 |
| 14 | 내 선물 만들기 (진입) | `/gift/create/my` | `GiftCreateMyPage` | 우준영 | ⚠️ 임시 라우트 (준비 중 placeholder, 이번 범위 밖) |
| 15 | 함께 선물 만들기 (진입) | `/gift/create/together` | `GiftCreateTogetherPage` | 우준영 | ⚠️ 임시 라우트 (준비 중 placeholder, 이번 범위 밖) |
| 16 | 펀딩 초대장 | `/funding/:id/invitation` | `InvitationPage` | 장하영 | 개설자가 커스텀한 초대장 조회, '축하하러 가기' 진입 |
| 17 | 펀딩 상세 조회 | `/funding/:id` | `FundingDetailPage` | 장하영 | 펀딩 진행률·위시·메세지 조회 (공개 범위 설정에 따라 조건부 표시, 개설자/참여자 뷰 분기) |
| 18 | 펀딩 수정 (선택) | `/funding/:id/edit` | `FundingEditSelectPage` | 장하영 | 수정할 항목 선택 |
| 19 | 펀딩 수정 (단계) | `/funding/:id/edit/:step` | `FundingEditStepPage` | 장하영 | 선택한 항목 단계별 수정 |
| 20 | 축하 메세지 전체보기 | `/funding/:id/messages` | `MessagesPage` | 장하영 | 축하 메세지 전체 조회 |
| 21 | 펀딩 참여 (4단계) | `/funding/:id/participate` | `ParticipatePage` | 장하영 | 이름→편지→금액→입금의 4단계 참여 흐름 |
| 22 | 참여 완료 | `/funding/:id/complete` | `CompletePage` | 장하영 | 참여 완료 후 축하 화면 표시 |
| 23 | 선물 후기 작성 | `/gift/review/write/:type` | `ReviewWritePage` | 우준영 | 3유형(선물 후기/소식 전하기/마음 전하기) 작성 — 제목·내용·이미지·초대장 색상·캐릭터 선택 |
| 24 | 선물 후기 작성 완료 | `/gift/review/complete/:type` | `ReviewCompletePage` | 우준영 | 작성 완료 화면 — 링크 복사·공유·미리보기·닫기 |
| 25 | 선물 후기 조회 | `/gift/review/:id` | `GiftReviewDetailPage` | 우준영 | 이미지 캐러셀(n/N + 도트) + 편지지 본문 조회 (배경색 반영) |

> **바텀시트·모달 등 라우트 없는 하위 UI** (부모 페이지 상태로 노출):
> `TermsBottomSheet`(약관), `PriceFilterSheet`·`WishTypeSheet`(홈 필터/위시), `WishEditModeSheet`(위시 카드 "⋮" 수정·삭제 선택), `PhotoSourceSheet`·`PhotoCropOverlay`(프로필 사진 등록 방식 선택/원형 자르기), `GiftCreateSheet`·`WishCreateSheet`(선물 만들기 진입 시트, 우준영), `PhotoActionSheet`·`ImageCropper`(후기 이미지 등록/자르기), `BankSelectSheet`(은행 선택), `DateSheet`(날짜 선택), `LetterModal`(편지 열람), `ConfirmModal`(로그아웃·삭제·이탈 확인).

> ⚠️ 14·15번 `/gift/create/*`는 현재 "준비 중" placeholder입니다. 실제 선물 만들기 진입은 **홈 → 진입 바텀시트(`GiftCreateSheet`) → `/funding/create`(12번)** 흐름으로 동작하며, `/gift/about`(13번)의 하단 CTA도 `/funding/create`로 연결됩니다.

---

## 3. 라우팅 구조

`src/main.tsx`에서 `QueryClientProvider` → `BrowserRouter` → `AuthProvider` 순으로 앱을 감싸고, `src/App.tsx`에서 `Routes`/`Route`로 경로를 정의합니다.

```tsx
// src/main.tsx
<QueryClientProvider client={queryClient}>
  <BrowserRouter>
    <AuthProvider>
      <App />
    </AuthProvider>
  </BrowserRouter>
</QueryClientProvider>
```

```tsx
// src/App.tsx (발췌)
<Routes>
  <Route path="/"                        element={<SplashPage />} />
  <Route path="/login"                   element={<LoginPage />} />
  <Route path="/signup/profile"          element={<ProfileSetupPage />} />
  <Route path="/home"                    element={<HomePage />} />
  <Route path="/wish"                    element={<WishPage />} />
  <Route path="/wish/:id/edit"           element={<WishEditPage />} />
  <Route path="/my"                      element={<MyPage />} />
  <Route path="/my/profile"              element={<ProfileEditPage />} />
  <Route path="/my/accounts"             element={<AccountListPage />} />
  <Route path="/my/accounts/new"         element={<AccountFormPage />} />
  <Route path="/my/accounts/:id/edit"    element={<AccountFormPage />} />
  <Route path="/funding/create"          element={<FundingCreatePage />} />
  <Route path="/gift/about"              element={<GiftAboutPage />} />
  <Route path="/gift/create/my"          element={<GiftCreateMyPage />} />
  <Route path="/gift/create/together"    element={<GiftCreateTogetherPage />} />
  <Route path="/funding/:id/invitation"  element={<InvitationPage />} />
  <Route path="/funding/:id"             element={<FundingDetailPage />} />
  <Route path="/funding/:id/edit"        element={<FundingEditSelectPage />} />
  <Route path="/funding/:id/edit/:step"  element={<FundingEditStepPage />} />
  <Route path="/funding/:id/messages"    element={<MessagesPage />} />
  <Route path="/funding/:id/participate" element={<ParticipatePage />} />
  <Route path="/funding/:id/complete"    element={<CompletePage />} />
  <Route path="/gift/review/write/:type"    element={<ReviewWritePage />} />
  <Route path="/gift/review/complete/:type" element={<ReviewCompletePage />} />
  <Route path="/gift/review/:id"            element={<GiftReviewDetailPage />} />
</Routes>
```

> 라우팅 방식: `BrowserRouter`(History API) 기반. 동적 파라미터는 `/funding/:id`, `/gift/review/:id`, `/gift/review/write/:type` 등에서 사용합니다.
> SPA 새로고침 대응을 위해 배포 환경(Vercel)에서 SPA fallback 처리가 필요합니다.

---

## 4. 화면별 진입 경로

| 화면 | 진입 경로 (트리거) |
| --- | --- |
| 스플래시 `/` | 앱 최초 진입 (URL 루트) |
| 로그인 `/login` | ① 스플래시 2초 후 자동 이동(`replace`) ② 마이페이지에서 비로그인 상태로 프로필 클릭 시 |
| 프로필 설정 `/signup/profile` | 로그인에서 소셜 로그인 후 신규 가입자(`isNewUser`)일 때 |
| 홈 `/home` | ① 프로필 설정 완료 ② 하단 네비게이션 "홈" 탭 ③ 각종 완료/나가기 후 복귀 |
| 위시 `/wish` | 하단 네비게이션 "위시" 탭 |
| 위시 수정 `/wish/:id/edit` | 위시 카드 "⋮" 액션 시트(`WishEditModeSheet`)에서 "수정하기" 선택 |
| 선물 만들기 `/funding/create` | ① 홈 진입 바텀시트(`GiftCreateSheet`)의 만들기 선택 ② `/gift/about` 하단 CTA |
| 이용 방법 `/gift/about` | 홈/진입 시트의 "이용 방법" 안내 링크 |
| 마이페이지 `/my` | 하단 네비게이션 "마이" 탭 |
| 내 정보 수정 `/my/profile` | 마이페이지에서 로그인 상태로 프로필 영역 클릭 |
| 계좌 목록 `/my/accounts` | 마이페이지 메뉴 "계좌 관리" |
| 계좌 등록/수정 `/my/accounts/new`·`/:id/edit` | 계좌 목록에서 "새로운 계좌 등록하기" / 개별 계좌 수정 |
| 펀딩 초대장 `/funding/:id/invitation` | 공유받은 초대장 링크 |
| 펀딩 상세 `/funding/:id` | ① 초대장 "축하하러 가기" ② 만들기 완료 후 "선물 페이지 보기"(`?owner=1` 개설자 뷰) |
| 펀딩 수정 `/funding/:id/edit` | 펀딩 상세(개설자 뷰)에서 "수정하기" |
| 축하 메세지 전체 `/funding/:id/messages` | 펀딩 상세에서 메세지 "전체보기" |
| 펀딩 참여 `/funding/:id/participate` | 펀딩 상세에서 "축하하러 가기" |
| 참여 완료 `/funding/:id/complete` | 참여 4단계 완료 |
| 선물 후기 작성 `/gift/review/write/:type` | E(내 선물 참여)·H(함께 선물) 완료 화면의 후기/소식/마음 남기기 진입 (앞 화면은 E·H 담당) |
| 후기 작성 완료 `/gift/review/complete/:type` | 후기 작성 화면 "저장" |
| 후기 조회 `/gift/review/:id` | 완료 화면 "미리보기" / 공유 링크 |

---

## 5. 페이지별 담당자

> `git log` 커밋 작성자 기준 (snow-jun-0 · Jun = 우준영). `dev` 병합 완료 화면과 미완/후속 영역을 구분해 표기했습니다.

| 담당자 | 담당 영역 | 대표 화면·모듈 |
| --- | --- | --- |
| 이홍연 | 온보딩(A/B)·홈·위시(F)·마이(I)·계좌 + 공통 인프라 | 스플래시/로그인/프로필설정/홈/위시/마이/내정보수정/계좌, 공통 컴포넌트, API 인프라(`api/`, `lib/`) |
| 최수민 | 선물 만들기 플로우(D/G) | `FundingCreatePage`, `components/create/Step1~5`, `store/fundingCreateStore` |
| 우준영 | 선물 만들기 진입(C)·선물 후기(J) | `gift-about`, `gift-create`(진입 시트), `gift-review`(작성/완료/조회) |
| 장하영 | 초대장(E01)·펀딩 상세/참여(E)·수정·메세지 | `invitation`, `funding`(상세/수정/메세지), `participate` |

> 📌 To-do: `/gift/create/my`·`/gift/create/together`(진입 후 실제 플로우 연결)는 현재 placeholder이며, `/funding/create` 플로우와의 최종 연결 방식 확정 필요.

---

## 6. 공통 컴포넌트 목록 및 사용 방식

`src/components/common` — 페이지 전반에서 재사용되는 UI 컴포넌트입니다. (실제 파일 기준 교차 검증 완료)

| 컴포넌트 | 설명 | 사용 위치 |
| --- | --- | --- |
| `Button` | 하단 CTA 버튼 (h52 / radius12, `disabled` 비활성) | 프로필 설정, 후기 작성, 펀딩 상세 등 전역 |
| `TextField` | 라벨 + 글자수 카운터 입력창 | 프로필 설정, 내 정보 수정, 후기 작성, 계좌 등록 |
| `Header` | 상단 네비게이션 바 (뒤로가기 + 타이틀 + 우측 슬롯) | 라우트 페이지 대부분 |
| `BottomSheet` | 하단에서 올라오는 시트 (딤 + 그래버) | 약관·가격필터·은행선택·날짜선택 등 |
| `BottomNav` | 하단 탭 네비게이션 + 플로팅(+) 버튼 | 홈, 위시, 마이페이지 |
| `MenuRow` | 메뉴 리스트 행 (우측 셰브런 옵션) | 마이페이지, 내 정보 수정 |
| `ConfirmModal` | 확인 모달 (동의 체크박스 옵션, 2버튼) | 로그아웃, 계정 삭제, 이탈·종료 확인 |
| `Toast` | 하단 스낵바 토스트 | 마이페이지, 펀딩 상세(링크 복사 등) |
| `LetterCard` | **편지지 카드 컴포넌트 (8색 팔레트, 접힘/입력전/열림 상태)** — E·J 공용 | 펀딩 참여(편지), 선물 후기(작성/조회) |
| `letterPalette.ts` | 편지지 8색 색상 토큰(`LETTER_COLORS`) 정의 | `LetterCard` 및 참여·후기 화면 |
| `DefaultAvatar` | 기본 프로필 아바타 | 프로필/마이 관련 화면 |

**아이콘 컴포넌트** (`src/components/icons`) — SVG를 React 컴포넌트로 래핑, `className`으로 색상/크기 제어.
Home/Gift/Profile/Plus/Search/Check/Close/CaretDown/ChevronLeft·Right/Star/Wallet/Bank/Chat/Message/Link/Share/Lock/LogOut/Expand/Edit/More/CircleCheck/ArrowUpRight 등. (일부 아이콘은 `lucide-react` 병용)

**생성 플로우 컴포넌트** (`src/components/create`, 최수민) — 선물 만들기 5단계 및 이미지 처리.
`StepIndicator`, `Step1BasicInfo`~`Step5Invite`, `StepComplete`, `DateSheet`, `PhotoActionSheet`, `ImageCropper`, `Mascot`.
> `PhotoActionSheet`(등록 방식 시트)와 `ImageCropper`(canvas 크롭)는 선물 후기 이미지 등록(J)에서도 그대로 재사용됩니다.

> 🎨 디자인 토큰: 색상/타이포는 `src/index.css`의 Tailwind `@theme`에 정의. 하드코딩 대신 `text-gray-900`, `bg-pink-500`, `text-h3-sb`, `text-b2-m` 등 토큰 클래스를 사용합니다.

---

## 7. 데이터 관리 (Mock / API 연동 여부)

`@tanstack/react-query`·`axios`·`zustand`가 모두 설치·설정되어 있으며, 일부 도메인은 **실제 백엔드 API와 연동**되고 나머지는 Mock으로 렌더링됩니다.

| 도메인 | 연동 상태 | 위치 / 비고 |
| --- | --- | --- |
| 사용자 프로필 조회·수정·탈퇴 | ✅ **API 연동** | `api/users.ts`, `hooks/useMyProfile.ts` (`GET/PATCH/DELETE /api/v1/users/me`) |
| 등록 계좌 CRUD | ✅ **API 연동** | `api/userAccounts.ts`, `hooks/useUserAccounts.ts` (`/api/v1/user-accounts`) |
| 토큰 자동 갱신 | ✅ **API 연동** | `lib/apiClient.ts` 401 인터셉터 (`/api/v1/auth/tokens/refresh`) |
| 로그아웃 | 🟡 `dev`는 클라이언트 처리만 | `pages/my/ProfileEditPage.tsx` `handleLogout` — 서버 로그아웃 API가 명세에 없어 `clearTokens()` + `logout()`으로 로컬 토큰만 정리. 실제 `/api/v1/auth/tokens/me` DELETE 연동은 `feat/#131-logout-api` 브랜치에서 구현 완료, `dev` 미병합 |
| 소셜 로그인 | 🟡 `dev` 기준 미연동 | `api/auth.ts` (`/api/v1/auth/tokens/{provider}`) — 구글(`feat/#104-google-login`)·카카오(`feat/#129-kakao-login`) 모두 클라이언트 SDK 연동까지 FE 구현 완료됐으나 `dev` 미병합. 백엔드 이슈(구글: DB 미구축 / 카카오: 토큰 검증 오류 `USER401_1`)로 실제 로그인은 미검증 |
| 내 펀딩 목록 | ✅ **API 연동** | `api/users.ts` `getMyFundings`, `pages/home/useMyFundings.ts` (`GET /api/v1/users/me/fundings`) |
| 상품·위시 | ❌ Mock | `pages/home/products.ts`, `store/wishStore.ts` (TODO: BE 연동 시 API 교체) |
| 펀딩 상세·참여·메세지 | ❌ Mock | `pages/funding/fundingMock.ts`·`participantMock.ts`·`editFundingMock.ts` |
| 선물 만들기 입력값 | ❌ 로컬(Zustand) | `store/fundingCreateStore.ts` — 만들기 완료 시 `commitAsFunding`으로 상세에 반영 |
| 선물 후기 | ❌ Mock/전달 | `pages/gift-review/mockReview.ts`, 작성→조회는 `navigate` state로 전달 |

**API 공통 규약** — 모든 응답은 `ApiEnvelope<T>` 형태(`{ isSuccess, code, message, result }`)이며, `unwrap()` 헬퍼가 `result`만 반환하고 실패 시 `ApiError`를 던집니다.
백엔드 베이스 URL: `import.meta.env.VITE_API_BASE_URL`(기본값 `http://43.201.153.143:8080`).

---

## 8. 상태 관리 방식

| 범위 | 방식 | 구현 |
| --- | --- | --- |
| 인증(로그인) 상태 | **Context API** | `contexts/AuthProvider.tsx` — `isLoggedIn`, `login()`, `logout()`. `hooks/useAuth.ts`로 소비. `apiClient`의 `onAuthExpired`를 구독해 강제 로그아웃 처리 |
| 서버 상태 | **TanStack Query** | `lib/queryClient.ts` + `QueryClientProvider`. `useMyProfile`, `useUserAccounts` 등 `useQuery` 훅 |
| 클라이언트 전역 상태 | **Zustand** | `store/fundingCreateStore.ts`(만들기 5단계 입력·수정 스냅샷), `store/wishStore.ts`(위시 토글) |
| 지역(UI) 상태 | **useState** | 시트/모달 열림, 스텝 인덱스, 필터 선택, 캐러셀 인덱스 등 |
| 화면 간 데이터 전달 | **Router state** | 후기 작성→완료→조회 데이터 전달, 마이페이지 토스트 메시지 등 |
| 토큰 저장 | **localStorage** | `lib/tokenStorage.ts` (`toget_access_token` / `toget_refresh_token`) |

> ⚠️ `AuthProvider`의 로그인 초기값이 개발 편의상 `true`로 설정되어 있습니다. 실제 인증 연동 시 `false`로 변경 + 로그인 성공 시 토큰 저장 흐름과 연결이 필요합니다. (코드 TODO 명시)

---

## 9. 트러블 슈팅 기록

> 노션 [트러블 슈팅 목록](https://app.notion.com/p/ef9ac56ca078822da919011f40ed5bf4) 기준 실제 팀 기록 사례입니다. (양식은 팀 트러블 슈팅 템플릿 기준)

### TS-01. 한글 입력 시 글자수 제한(maxLength) 초과 입력

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | `TextField`에 `maxLength`를 지정해도 한글 조합(IME) 입력 중에는 브라우저가 제한을 강제하지 않아 최대 글자수를 초과 입력됨 |
| 발생 시점 | 프로필 설정 / 내 정보 수정 닉네임 입력 |
| 담당자 | 이홍연 |
| 원인 | IME 조합(composition) 중에는 `maxLength` 속성이 적용되지 않는 브라우저 동작 |
| 해결 방법 | `onChange` 핸들러에서 `value.length > maxLength`인 경우 `slice(0, maxLength)`로 직접 잘라냄 |
| 참고 자료 | `components/common/TextField.tsx` |

### TS-02. 토스트가 새로고침 시 다시 노출되는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 로그아웃/계정삭제 후 마이페이지로 이동하며 토스트를 띄우는데, 새로고침 시 토스트가 재노출될 수 있음 |
| 발생 시점 | 내 정보 수정 → 로그아웃/계정삭제 완료 이동 |
| 담당자 | 이홍연 |
| 원인 | 토스트 메시지를 `navigate` state로 전달하는데, state가 히스토리에 남아 재사용됨 |
| 해결 방법 | 마운트 시 메시지를 읽은 뒤 `navigate(pathname, { replace: true, state: null })`로 state를 즉시 소거하고 일정 시간 후 숨김 처리 |
| 참고 자료 | `pages/my/MyPage.tsx` |

### TS-03. 머지 충돌 해결 실수로 코드 중복·손상 발생

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 여러 PR을 dev에 머지하는 과정에서 코드가 중복 삽입되어 하단 탭바가 2개 렌더링되고 dev 브랜치 빌드가 실패함 (App.tsx 라우트 중복, GiftAboutPage 코드 이중 삽입, HomePage BottomNav 중복) |
| 발생 시점 | 다수 PR dev 머지 시점 |
| 담당자 | 우준영 |
| 원인 | 충돌 해결 시 `Accept both changes`를 눌러 양쪽 변경분이 통째로 삽입됨. 머지 이후 dev 빌드를 확인하는 담당이 없어 깨진 채 방치됨 |
| 해결 방법 | hotfix 브랜치로 즉시 복구. 이후 `Accept both changes` 사용 금지(수동 병합), 머지 후 dev 빌드 확인을 루틴화 |
| 참고 자료 | `App.tsx`, `pages/gift/GiftAboutPage.tsx`, `pages/home/HomePage.tsx` |

### TS-04. Approved된 PR이 머지되지 않고 누적되는 병목

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | Open PR 10건이 전부 `status:review` 상태로 쌓이고, 리뷰어 Approve를 받은 PR도 5일간 머지되지 않아 후속 작업이 막힘 |
| 발생 시점 | 2026.07.19 |
| 담당자 | 우준영 (팀장) |
| 원인 | 리뷰어 승인 후에도 별도 머지 담당이 없어 대기만 하는 구조. 프로세스가 흐르지 않고 정체됨 |
| 해결 방법 | 팀 합의로 작성자 본인이 승인된 PR을 직접 머지할 수 있도록 규칙 완화. 프로세스는 지키기 위함이 아니라 흐르게 하기 위한 것으로 재정의 |
| 참고 자료 | GitHub `UMC-toget/ToGet-FE` PR 목록 |

### TS-05. 컨벤션 문서와 실제 레포 구현의 불일치

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 컨벤션 문서에는 TanStack Query·Zustand·`features/도메인/` 구조가 명시돼 있으나, 실제 레포는 미설치 상태이며 `src/pages/<도메인>/` + `src/components/common/` 구조로 되어 있어 신규 팀원이 혼란을 겪음 |
| 발생 시점 | 1차 과제 피드백 반영 시점 |
| 담당자 | 우준영 (팀장) |
| 원인 | 초기 컨벤션 문서가 BE 기준(PayLinker 기반)으로 작성됨. 문서를 먼저 쓰고 코드가 따라가지 못해 괴리 발생 |
| 해결 방법 | 컨벤션 문서를 실제 FE 레포 기준으로 전면 재작성, 공용 컴포넌트 관리표 최신화. 코드→문서 반영 주기를 짧게 가져가기로 함 |
| 참고 자료 | 프론트 코딩 컨벤션 문서 (노션) |

### TS-06. 디자인 위치 오인으로 캐릭터 선택 UI를 재구현

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | J파트 후기 작성 화면의 캐릭터 선택 UI를 구현하는데, 피그마에 해당 디자인이 없는 줄 알고 E파트(Step5Invite) 패턴을 가져와 임의 구현했다가, 이후 실제 디자인을 발견해 다시 맞춰야 했음 |
| 발생 시점 | 이슈 #103 작업 중 |
| 담당자 | 우준영 |
| 원인 | 'J01-1) 후기: 초대장 만들기' 프레임에 실제 디자인이 있었으나 초기에 찾지 못함 (피그마 파일 구조상 초기 디자인 페이지에 분산 배치) |
| 해결 방법 | 실제 프레임 확인 후 재구현 — 헤더 '후기 남기기', 미리보기는 초대장 카드(ToGet 로고+캐릭터+편지지+확대 버튼), 색상 탭 '초대장 색상'(8색), 캐릭터 탭 좌우 화살표+No.01~06. 캐릭터 에셋은 기존 `invite-character1~6.svg` 재사용 |
| 참고 자료 | 이슈 #103, 피그마 'J01-1' 프레임 |

### TS-07. blob URL이 조회 화면에서 깨지는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 선물 후기 작성 → 완료 → 조회로 화면을 넘길 때 등록한 이미지가 조회 화면에서 표시되지 않음 |
| 발생 시점 | 이슈 #119 (후기 조회 화면 데이터 반영) 작업 중 |
| 담당자 | 우준영 |
| 원인 | 화면 간 이미지를 `navigate` state로 전달하는데, blob URL이 페이지 전환 후 revoke되어 무효화됨 |
| 해결 방법 | 이미지를 base64 data URL로 변환해 state에 담아 전달 → revoke 문제 회피. state가 없을 경우 `mockReview` 폴백 처리 |
| 참고 자료 | 이슈 #119, `pages/gift-review/ReviewWritePage.tsx`, `GiftReviewDetailPage.tsx` |

### TS-08. 히어로 이미지 비율이 깨지는 문제

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 후기 작성 완료 화면의 상단 히어로 영역을 개별 요소로 조립했더니 화면에서 비율이 맞지 않고 깨짐 |
| 발생 시점 | 이슈 #85 (후기 작성 완료 화면) 작업 중 |
| 담당자 | 우준영 |
| 원인 | 여러 레이어를 코드로 개별 조립하면서 피그마 원본 비율을 정확히 재현하지 못함 |
| 해결 방법 | 조립 대신 피그마에서 통짜 이미지로 export(`review-complete-hero.png`, 2x)해 교체 → 비율 문제 해결 |
| 참고 자료 | 이슈 #85, `review-complete-hero.png` |

### TS-09. 카카오톡 소셜 로그인 Redirect URL, KOE010 에러 해결하기

| 항목 | 내용 |
| --- | --- |
| 문제 상황 | 카카오 소셜 로그인 시도 시 네트워크 응답에 "Bad client credentials"와 함께 `KOE010` 에러 코드 반환 |
| 발생 시점 | 카카오 로그인 클라이언트 SDK 연동 (`feat/#129-kakao-login`) |
| 담당자 | 이홍연 |
| 원인 | 카카오 디벨로퍼스 앱의 Client Secret이 활성화돼 있었는데 클라이언트 요청에서 이 값을 전송하지 않아, 토큰 요청 시 필수 파라미터가 누락됨 |
| 해결 방법 | 카카오 디벨로퍼스에서 REST API Key·JavaScript 키 모두에 로그인 Redirect URL 등록, REST API Key 수정 메뉴에서 Client Secret의 카카오 로그인 토글을 꺼서 인증 없이 진행되도록 변경 |
| 참고 자료 | [카카오 소셜 로그인 KOE010 에러 해결기](https://hongyeonlee.tistory.com/109) |

---

## 10. 배포

| 항목 | 내용 |
| --- | --- |
| 배포 플랫폼 | Vercel |
| 자동화 | GitHub Actions — `.github/workflows/preview.yaml`(PR Preview), `deploy.yml`(main push) |
| Preview 동작 | `main`/`dev` 대상 PR 생성 시 Vercel Preview 빌드 후 PR에 미리보기 URL 자동 코멘트 |
| 환경 변수 | `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`(Secrets), `VITE_API_BASE_URL`(백엔드 주소) |
| **배포 주소** | https://to-get-fe.vercel.app |

**배포 확인 체크리스트**

- [ ] 배포 링크 외부 접속 가능
- [ ] 주요 화면 정상 렌더링 (스플래시/로그인/홈/위시/마이/후기)
- [ ] 라우팅·새로고침 정상 작동 (SPA fallback — `/funding/1` 등 새로고침 시 404 여부)
- [ ] 모바일/태블릿/데스크톱 레이아웃 확인 (max-width 402px 중심)
- [ ] API 호출 정상 (프로필·계좌) / mixed-content 차단 여부 확인
- [ ] 콘솔 치명적 에러 없음

---

## 부록. 문서 ↔ 코드 정합성 점검 결과

| 점검 항목 | 상태 | 비고 |
| --- | --- | --- |
| Tech Stack 버전 명시 | ✅ 반영 | React 19.2.7 / TS 6.0.2 / Vite 8.1.1 / Tailwind 4.3.2 / RR 7.18.1 |
| TanStack Query / Zustand / axios | ✅ **설치·연동 완료** | 기존 "미설치" 서술 정정. 서버상태=TanStack Query, 클라이언트=Zustand, 통신=axios |
| API 연동 여부 | ✅ 부분 연동 | 프로필·계좌·토큰 갱신·내 펀딩 목록 실연동. 로그아웃·소셜 로그인(구글/카카오)은 FE 구현 완료 브랜치가 `dev` 미병합 상태(각 `feat/#131`·`feat/#104`·`feat/#129`), 펀딩 생성/상세/상품 도메인은 미연동 |
| 공통 컴포넌트 목록 최신화 | ✅ 반영 | `LetterCard`·`letterPalette`·`DefaultAvatar` 및 create 컴포넌트 추가 반영 |
| 화면 목록 최신화 | ✅ 반영 | 6화면 → 25라우트로 확장 (위시 수정/계좌/펀딩/후기 포함) |
| 담당자 매핑 | ✅ 반영 | `git log` 작성자 기준(홍연/수민/준영/하영) |
| 트러블 슈팅 | ✅ 9건 기록 | 노션 트러블 슈팅 목록 기준 (TS-01~09) |
