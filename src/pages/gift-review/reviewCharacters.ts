import char1 from '../../assets/invite-character1.svg'

/**
 * 서버 캐릭터 목록(useCharacters) 조회 실패 시 화면이 깨지지 않도록 쓰는 최소 폴백 이미지.
 * E파트 초대장 캐릭터(src/components/create/Step5Invite.tsx)와 동일한 에셋을 공용으로 사용한다.
 */
export const FALLBACK_CHARACTER_IMAGE = char1
